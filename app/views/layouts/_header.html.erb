<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->
  </head>

  <body class="black-depauw">
    <div class="container">
      <ul class="nav nav-tabs" role="tablist">
        <li> <a href="/"> <span class="glyphicon glyphicon-home"></span>  Home</a> </li>
        <li> <a href="/rides/new"> <span class="glyphicon glyphicon-plus"></span>  Request a Ride </a> </li>
        <% if current_user %>
        <li>
          <%= link_to user_path(id: current_user.id) do %>
            <span class="glyphicon glyphicon-th"></span> My Drive
          <% end %>
        </li>
        <% end %>
        <li>
          <% if user_signed_in? %>
            Logged in as <strong><%= current_user.email %></strong>.
            <%= link_to 'Edit profile', edit_user_registration_path, class: "login_button"  %> |
            <%= link_to "Logout", destroy_user_session_path, class: "login_button", method: :delete %>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, class: "login_button" %> |
            <%= link_to "Login", new_user_session_path, class: "login_button" %>
          <% end %>
      </ul>
    </div>
  <header class="sb-page-header">
    <div class="container">
      <h1 class="row text-center col-xs-6 col-xs-offset-3">DEPAUW RIDES</h1>
    </div>
  </header>
</body>
</html>
