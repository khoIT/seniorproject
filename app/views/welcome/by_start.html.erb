<%= render partial: "layouts/header" %>
  <div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading">Available Rides</div>

  <!-- Table -->
  <table class="table">
    <thead>
      <th>#</th>
      <th onclick="sort_by_start();">From</th>
      <th>Destination</th>
      <th>Departure</th>
      <th>Seats</th>
    </thead>
    <tbody>
    <% @all_rides.each do |r| %>
      <tr>
      <td><%= r.id %></td>
      <td><%= r.start %></td>
      <td><%= r.destination %></td>
      <td><%= r.seats_left %></td>
    </tr>
    <% end %>
    </tbody>
  </table>
  </div>
<script>
  function sort_by_start(){
  //get data from json
  //render it through a new template
}
</script>
